# ORCE track - local runtime using the published ORCE image only.
services:
  orce:
    image: ecofacis/xfsc-orce:2.0.13
    platform: linux/amd64
    ports:
      - "1880:1880"
    volumes:
      - orce_data:/data
    init: true
    restart: unless-stopped

volumes:
  orce_data:
