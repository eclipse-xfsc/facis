# ORCE track - local Node-RED runtime (no cloud deployment is provided)
services:
  node-red:
    image: nodered/node-red:4.0
    ports:
      - "1880:1880"
    volumes:
      - node_red_data:/data
    restart: unless-stopped
volumes:
  node_red_data:
